# 导出模板示例 2：双行标题
# 注意：columns 和 column_descriptions 由当前运行的组态决定，不在模板中配置
# filter_sampled_only 永远为 True，只导出采样周期的数据
# 第二行标题使用默认描述"某工业数据"
name: prediction
time_column_name: timeStamp
time_format: "%Y-%m-%d %H:%M:%S"
header_rows: 2

